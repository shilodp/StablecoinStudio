function ResultsRow({ label, value }) {
    return (
        <div className="resuilts-row">
            <div className="results-label">{label}</div>
            <div className="results-value">{value}</div>
        </div>
    );
}

export default ResultsRow;
